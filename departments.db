CREATE TABLE Departments (
    department_id   INTEGER    PRIMARY KEY
                               UNIQUE,
    department_name TEXT (255),
    manager_id      INTEGER    REFERENCES Employees (employeeID),
    location_id     INTEGER    REFERENCES Locations (location_id) 

);
